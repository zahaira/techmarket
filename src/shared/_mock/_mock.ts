import { _deviceDescriptions } from "./assets";

// ----------------------------------------------------------------------

export const _mock = {
  deviceDescription: (index: number) => _deviceDescriptions[index],
};
